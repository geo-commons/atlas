from django.apps import AppConfig

# TODO: Rename app name 'tables_v2' to a better name (maybe just tables?)
class TablesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "tables_v2"
